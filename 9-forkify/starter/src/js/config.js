//this file is not needed as the API url has changed and the new one does not require apikey and proxy, but I created it in case we need it for something else in the future as well.

export const proxy = '';
export const key = '';